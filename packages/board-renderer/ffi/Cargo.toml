[package]
name = "board-renderer-ffi"
version = "0.1.0"
edition = "2024"
description = "C FFI bindings for the board overlay renderer"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
board-renderer-core = { path = "../core" }
serde_json = "1"
