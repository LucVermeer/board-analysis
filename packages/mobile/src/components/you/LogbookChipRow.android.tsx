// Android placeholder: importing the SwiftUI-only .ios row here would pull
// @expo/ui/swift-ui onto the Android bundle. Android keeps the sheet's filter/sort.

import type { LogbookChipRowProps } from './LogbookChipRow.types';

export function LogbookChipRow(_props: LogbookChipRowProps): null {
  return null;
}
